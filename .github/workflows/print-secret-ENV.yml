name: Print env secret ENV

on:
  workflow_dispatch:

jobs:
  print-secret-ENV:
    runs-on: ubuntu-latest
    steps:
      - name: Print env secret ENV
        run: echo "---:${{ secrets.ENV }}:---"
      
